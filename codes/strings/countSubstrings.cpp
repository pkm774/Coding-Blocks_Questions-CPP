#include <iostream>
using std::cin;
using std::cout;
using std::endl;
using std::string;

#include <string>

int countSubstrings(string s) {
	auto len = s.length();
	cout<<
	return len;
}

int main() {
	string input = " ";

	getline(cin, input);

	cout << countSubstrings(input) << endl;

	return 0;
}
